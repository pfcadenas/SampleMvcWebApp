@model IEnumerable<SampleWebApp.Models.TopWordsViewModels>

@{
    Layout = "";
}
@if (Model.Count() > 0)
{
    <h3>Top Five</h3>

    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Word (@item.Weigth)</li>
        }
    </ul>

}